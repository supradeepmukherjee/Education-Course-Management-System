import { } from 'react'
import { useParams } from 'react-router-dom'
import './Courses.css'

const Courses = () => {
    const { name } = useParams()
    return (
        <div>
            
        </div>
    )
}

export default Courses